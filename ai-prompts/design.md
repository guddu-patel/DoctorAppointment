# Prompt — Design

Design a single owned appointment status state machine module. Include allowed transitions table and role permissions. Require that services call it; forbid UI-only enforcement. Propose file path under `backend/src/domain/`.
