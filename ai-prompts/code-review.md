# Prompt — Code Review

Audit the diff against `docs/acceptance-criteria.md`. Flag any path that updates appointment status without `assertAppointmentStatusChange`. Separate Accepted vs Rejected recommendations with rationale.
