# Prompt — Debugging

Given symptom + error message (no secrets), list root-cause hypotheses ordered by likelihood, then the smallest verification step. After fix, draft an entry for `docs/debugging-notes.md`.
