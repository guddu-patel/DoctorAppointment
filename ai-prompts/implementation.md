# Prompt — Implementation

Implement booking with slot conflict detection and wire status updates through `assertAppointmentStatusChange`. Match existing Express layering and Zod validation. Do not add new frameworks.
